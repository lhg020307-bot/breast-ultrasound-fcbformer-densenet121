# checkpoints

Place local pretrained weights and trained checkpoints here.

Expected pretrained segmentation checkpoint:

```text
checkpoints/pretrained/FCBFormer_checkpoint.pt
```

Model checkpoint files are ignored by Git because they are large binary
artifacts. Use GitHub Releases, cloud storage, or a model registry if you need
to distribute weights.
