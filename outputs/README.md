# outputs

This directory is for generated logs, trained model outputs, metrics, figures,
reports, and intermediate prediction files.

The contents are ignored by Git. Recreate them by running the training,
evaluation, fusion, or frontend scripts described in the root README.
